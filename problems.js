// Get The Result Function
/* 
const getResult = (numbers) => {
  return numbers.reduce((acc, curr) => acc + curr, 0);
};

console.log(getResult([1, 2, 3, 4, 5]));
*/

// Get The Biggest Number
/* 
const getTheBiggest = (numbers) => {
  return Math.max(...numbers);
};

console.log(getTheBiggest([1, 2, 6, 7, 144, 255]));
*/

// Reverse The String
/* 
function reverseString(string) {
  return string.split("").reverse().join("");
}

console.log(reverseString("ahmed"));
*/

// Remove Repeating
/* 
function removeRepeats(numbers) {
  //   return Array.from(new Set(numbers));
  return [...new Set(numbers)];
}

console.log(
  removeRepeats([
    1, 2, 3, 4, 2, 2, 2, 2, 2, 2, 2, 3, 4, 5, 6, 7, 7, 7, 7, 7, 7, 7, 7,
  ])
);
*/
